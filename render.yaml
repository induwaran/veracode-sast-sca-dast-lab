services:
  - type: web
    name: veracode-sast-sca-dast-lab
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: BASE_URL
        sync: false
      - key: SESSION_SECRET
        generateValue: true
      - key: ADMIN_EMAIL
        sync: false
      - key: LOCAL_AUTH_ENABLED
        value: "true"
      - key: LOCAL_USER_PASSWORD
        sync: false
      - key: LOCAL_ADMIN_PASSWORD
        sync: false
      - key: SAML_ENTRY_POINT
        sync: false
      - key: SAML_ISSUER
        sync: false
      - key: SAML_IDP_CERT
        sync: false
      - key: APP_VERSION
        value: v1.6
